"use client";

import { memo } from "react";

/** Intermission overlay — no longer needed since shop auto-opens after wave clear.
 *  Kept as empty component to avoid breaking imports. */
export const IntermissionOverlay = memo(function IntermissionOverlay() {
  return null;
});
